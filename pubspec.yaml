name: shared
description: Shared code for FreshFarmily mobile apps
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  shared_preferences: ^2.2.2
  http: ^1.1.0
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.1
  carousel_slider: ^4.2.1
  provider: ^6.1.1
  intl: ^0.18.1
  google_maps_flutter: ^2.5.0
  flutter_polyline_points: ^2.0.0
  connectivity_plus: ^5.0.2
  path_provider: ^2.1.2
  uuid: ^4.3.1
  logging: ^1.2.0
  # Added authentication dependencies
  flutter_secure_storage: ^9.0.0
  google_sign_in: ^6.1.6
  image_picker: ^1.0.7
  page_transition: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
    - assets/
  
  # Commenting out the fonts section as we'll use system fonts for now
  # fonts:
  #   - family: Roboto
  #     fonts:
  #       - asset: assets/fonts/Roboto-Regular.ttf
  #       - asset: assets/fonts/Roboto-Medium.ttf
  #         weight: 500
  #       - asset: assets/fonts/Roboto-Bold.ttf
  #         weight: 700
